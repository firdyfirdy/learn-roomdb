<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firdy.kalbeapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.product.ProductAddActivity"></activity>
        <activity android:name=".view.customer.CustomerAddActivity" />
        <activity android:name=".view.pembelian.PembelianAddActivity" />
        <activity android:name=".view.brand.BrandAddActivity" />
        <activity android:name=".view.pembelian.PembelianActivity" />
        <activity android:name=".view.customer.CustomerActivity" />
        <activity android:name=".view.brand.BrandActivity" />
        <activity android:name=".view.product.ProductActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>